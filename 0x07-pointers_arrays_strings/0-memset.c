#include "main.h"
/**
 * _memset() a function to fills the first n bytes of memory area
 * @s: the pointer to the memory
 * @b: constant byte
 * @n: first byte of the memory to be filled
 * Return: The pointer s in returned
*/
char *_memset(char *s, char b, unsigned int n)
{
    
}